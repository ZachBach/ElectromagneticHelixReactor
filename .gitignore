node_modules/
.vercel/
.DS_Store
Thumbs.db

# LaTeX build artifacts (paper/)
# The compiled paper is build output, not source — it was committed until
# 2026-09-05 and had already drifted behind the .tex. Build it with tectonic.
# Note this ignores only the papers themselves: paper/figures/*.pdf are figures
# the .tex files \includegraphics, and stay tracked.
paper/ehr-paper.pdf
paper/pitch-confinement.pdf
*.aux
*.log
*.out
*.toc
*.bbl
*.blg
*.synctex.gz
*.fdb_latexmk
*.fls
*.xdv

# Personal notes and drafts (local only)
ZNA-todo-list.md
ZNA-linkedin/
